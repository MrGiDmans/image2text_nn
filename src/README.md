# 📘 Подробная документация к `config.yaml`

Ниже подготовлена подробная документация к каждому параметру `config.yaml`, чтобы понимать, что можно менять и к чему это приводит. Структура повторяет конфиг, но написана в виде удобной справки.

---

## **device**

**Тип:** `string`  
**Значение по умолчанию:** `"cuda"`

Тип устройства для обучения и инференса.

- `"cuda"` — использовать GPU (автоматически выберет доступный GPU)
- `"cpu"` — использовать процессор (медленнее, но работает везде)
- `"cuda:0"` — выбрать конкретный GPU (если несколько GPU)

**Рекомендации:**
- Используйте GPU для обучения (в 10-50 раз быстрее)
- CPU подходит только для тестирования небольших моделей

---

## **seed**

**Тип:** `integer`  
**Значение по умолчанию:** `42`

Фиксированное значение для воспроизводимости результатов.

**Влияет на:**
- Инициализацию весов модели
- Shuffle данных в DataLoader
- Разделение train/val
- Dropout (если фиксирован)

**Важно:** Для полной воспроизводимости также установите:
```python
torch.backends.cudnn.deterministic = True
torch.backends.cudnn.benchmark = False
```
(Это уже сделано в коде)

---

# 📂 data

## **images_dir**

**Тип:** `string`  
**Пример:** `"./data/flickr8k/images"`

Путь к директории, где лежат изображения датасета.

**Формат:** Относительный или абсолютный путь. Должна содержать файлы изображений (`.jpg`, `.png`, etc.).

---

## **caption_file**

**Тип:** `string`  
**Пример:** `"./data/flickr8k/captions.csv"`

Путь к CSV-файлу с подписями к изображениям.

**Формат CSV:**
```csv
image,caption
667626_18933d713e.jpg,"A child in a pink dress is climbing up a set of stairs in an entry way ."
667626_18933d713e.jpg,"A girl going into a wooden building ."
```

**Важно:**
- Каждая строка = одно изображение + одна подпись
- Одно изображение может иметь несколько подписей (несколько строк)
- Заголовок обязателен: `image,caption`

---

## **vocab_path**

**Тип:** `string`  
**Пример:** `"./vocab.pkl"`

Путь для сохранения/загрузки словаря токенов (`Vocabulary`).

**Поведение:**
- Если файл существует → загружается существующий словарь
- Если файла нет → создается новый словарь из всех подписей

**Формат:** JSON-файл с полями:
- `word_to_idx`: Словарь слово → индекс
- `freq_threshold`: Порог частоты (для справки)

---

## **image_size**

**Тип:** `integer`  
**Значение по умолчанию:** `224`

Размер изображения после предобработки (квадрат, в пикселях).

**Процесс:**
1. Resize до 256×256
2. CenterCrop до `image_size × image_size` (для валидации)
3. RandomCrop до `image_size × image_size` (для обучения)

**Рекомендации:**
- `224` — стандарт для ResNet/EfficientNet (оптимально)
- `256` — больше деталей, но медленнее
- `196` — быстрее, но меньше деталей
- `128` — очень быстро, но низкое качество

**Влияние на память:** Увеличение размера в 2 раза → память в 4 раза.

---

## **batch_size**

**Тип:** `integer`  
**Значение по умолчанию:** `32`

Количество примеров в одном батче.

**Влияние:**
- **Больше batch_size:**
  - Стабильнее градиенты
  - Быстрее обучение (лучше утилизация GPU)
  - Больше памяти VRAM
- **Меньше batch_size:**
  - Меньше памяти
  - Медленнее обучение
  - Менее стабильные градиенты

**Рекомендации:**
- GPU с 4GB VRAM: `16-32`
- GPU с 8GB VRAM: `32-64`
- GPU с 16GB+ VRAM: `64-128`

**Важно:** При `use_amp: true` можно использовать больший batch_size.

---

## **num_workers**

**Тип:** `integer`  
**Значение по умолчанию:** `4`

Количество процессов/потоков для параллельной загрузки данных.

**Влияние:**
- Больше workers → быстрее загрузка данных, но больше нагрузка на CPU
- Меньше workers → медленнее загрузка, но меньше нагрузка

**Особенности:**
- **Windows:** Автоматически устанавливается в `0` (из-за spawn multiprocessing)
- **Linux/Mac:** Оптимально `4-8`

**Рекомендации:**
- 4 ядра CPU: `2-4`
- 8+ ядер CPU: `4-8`
- Если GPU ждет данные → увеличьте `num_workers`

---

## **val_split**

**Тип:** `float`  
**Значение по умолчанию:** `0.2`

Доля данных для валидации (от 0.0 до 1.0).

**Примеры:**
- `0.2` = 20% валидация, 80% обучение
- `0.1` = 10% валидация, 90% обучение
- `0.3` = 30% валидация, 70% обучение

**Рекомендации:**
- Маленький датасет (< 5000 примеров): `0.1-0.15`
- Средний датасет (5000-20000): `0.2`
- Большой датасет (> 20000): `0.2-0.3`

**Важно:** Разделение происходит случайно, но с фиксированным `seed` для воспроизводимости.

---

## **freq_threshold**

**Тип:** `integer`  
**Значение по умолчанию:** `5`

Минимальная частота появления слова в датасете, чтобы оно было добавлено в словарь.

**Поведение:**
- Слова с частотой ≥ `freq_threshold` → добавляются в словарь
- Слова с частотой < `freq_threshold` → заменяются на `<UNK>`

**Влияние:**
- **Маленький порог (1-3):**
  - Большой словарь (10000+ слов)
  - Много редких слов
  - Модель хуже учится (шум)
- **Средний порог (5-10):**
  - Оптимальный баланс
  - Словарь 3000-5000 слов
  - Хорошее качество
- **Большой порог (20+):**
  - Маленький словарь (< 2000 слов)
  - Много `<UNK>` токенов
  - Модель не может описать редкие объекты

**Рекомендации:**
- Flickr8k: `5-10`
- Большой датасет (COCO): `3-5`
- Маленький датасет: `10-20`

---

# 🧠 model

## **embed_size**

**Тип:** `integer`  
**Значение по умолчанию:** `512`

Размер эмбеддинга слов (векторное представление слова).

**Влияние:**
- Больше размер → богаче представление, но больше параметров
- Меньше размер → быстрее, но беднее представление

**Рекомендации:**
- `256` — быстро, но низкое качество
- `512` — оптимально (баланс)
- `768` — лучше качество, но медленнее
- `1024` — максимальное качество, но очень медленно

**Память:** Увеличение в 2 раза → параметры Embedding в 2 раза больше.

---

## **decoder_dim**

**Тип:** `integer`  
**Значение по умолчанию:** `512`

Размер скрытого состояния LSTM-декодера.

**Влияние:**
- Больше размер → модель может запомнить больше контекста, но больше параметров
- Меньше размер → быстрее, но меньше "памяти"

**Рекомендации:**
- `256` — быстро, но низкое качество
- `512` — оптимально
- `768-1024` — лучше качество для сложных подписей

**Важно:** Обычно `decoder_dim = embed_size` для согласованности.

---

## **encoder_dim**

**Тип:** `integer`  
**Значение по умолчанию:** `512`

Размер выходных признаков энкодера изображения (после CNN).

**Влияние:**
- Должен совпадать с размером глобальных фич из EncoderCNN
- Используется для инициализации LSTM и конкатенации с эмбеддингами слов

**Рекомендации:**
- Обычно равен `decoder_dim` и `embed_size`
- Можно увеличить до `768` для лучшего качества, но потребуется больше памяти

---

## **dropout**

**Тип:** `float`  
**Значение по умолчанию:** `0.5`

Вероятность отключения нейронов во время обучения (регуляризация).

**Влияние:**
- `0.0` — нет регуляризации (риск переобучения)
- `0.3-0.5` — умеренная регуляризация
- `0.6-0.7` — сильная регуляризация (при переобучении)
- `0.8+` — слишком сильная (модель не учится)

**Рекомендации:**
- Начало обучения: `0.5`
- При переобучении: увеличьте до `0.6-0.7`
- При недообучении: уменьшите до `0.3-0.4`

**Важно:** Dropout работает только во время обучения (`model.train()`), отключается при инференсе.

---

## **encoder_fine_tune**

**Тип:** `boolean`  
**Значение по умолчанию:** `false`

Размораживать ли веса ResNet-50 энкодера для обучения.

**Режимы:**
- `false` — Encoder заморожен (используется как feature extractor)
  - Быстрее обучение
  - Меньше параметров для обучения
  - Меньше риск переобучения
  - Может быть недостаточно для специфичных данных
- `true` — Encoder разморожен (обучается вместе с декодером)
  - Медленнее обучение
  - Больше параметров
  - Выше риск переобучения
  - Может улучшить качество на специфичных данных

**Рекомендации:**
- Начните с `false` (быстрее и стабильнее)
- Если качество недостаточное → попробуйте `true` с маленьким `finetune_lr`

**Важно:** При `true` используйте `lr_mode: "finetune"` для разных LR.

---

## **encoded_image_size**

**Тип:** `integer`  
**Значение по умолчанию:** `14`

Размер feature map после CNN (в пикселях, квадрат).

**Процесс:**
1. ResNet-50 извлекает признаки размером `[B, 2048, H', W']`
2. AdaptiveAvgPool2d → `[B, 2048, 14, 14]`
3. Conv1x1 → `[B, encoder_dim, 14, 14]`
4. Reshape → `[B, 196, encoder_dim]` (196 = 14×14)

**Влияние:**
- Больше размер → больше пространственных признаков (для будущего attention)
- Меньше размер → меньше памяти, но меньше деталей

**Рекомендации:**
- `14` — оптимально для текущей архитектуры
- `7` — быстрее, но меньше деталей
- `28` — больше деталей, но больше памяти

**Важно:** В текущей реализации используется только глобальный вектор (mean pooling), пространственные признаки зарезервированы для будущего attention.

---

# 🚀 training

## **epochs**

**Тип:** `integer`  
**Значение по умолчанию:** `50`

Максимальное количество эпох обучения.

**Влияние:**
- Больше эпох → больше времени, но может улучшить качество
- Меньше эпох → быстрее, но может быть недостаточно

**Рекомендации:**
- Обычно обучение останавливается раньше из-за early stopping
- Установите большое значение (50-100), early stopping остановит при необходимости

---

## **lr**

**Тип:** `float` (научная нотация)  
**Значение по умолчанию:** `1e-4`

Базовая скорость обучения (learning rate) для декодера.

**Влияние:**
- Слишком большой LR → нестабильное обучение, loss не сходится
- Слишком маленький LR → медленное обучение
- Оптимальный LR → быстрая сходимость

**Рекомендации:**
- Начало: `1e-4` (0.0001)
- Если loss не уменьшается → уменьшите до `5e-5`
- Если обучение медленное → увеличьте до `2e-4` (осторожно!)

**Важно:** Используется для декодера. Для энкодера см. `finetune_lr`.

---

## **lr_mode**

**Тип:** `string`  
**Значение по умолчанию:** `"continue"`  
**Варианты:** `"continue"` или `"finetune"`

Режим обучения для энкодера и декодера.

**Режимы:**
- `"continue"` — Encoder и Decoder используют одинаковый LR (`lr: 1e-4`)
  - Подходит, когда `encoder_fine_tune: false` (encoder заморожен)
  - Или когда encoder уже предобучен и нужен одинаковый темп обучения
- `"finetune"` — Encoder использует `finetune_lr`, Decoder использует `lr`
  - Подходит, когда `encoder_fine_tune: true`
  - Encoder обучается медленнее (меньший LR), чтобы не "забыть" предобученные признаки

**Рекомендации:**
- `encoder_fine_tune: false` → используйте `"continue"`
- `encoder_fine_tune: true` → используйте `"finetune"` с `finetune_lr: 1e-5`

---

## **finetune_lr**

**Тип:** `float` (научная нотация)  
**Значение по умолчанию:** `1e-5`

Скорость обучения для энкодера при `lr_mode: "finetune"`.

**Влияние:**
- Обычно в 10 раз меньше, чем `lr` (для декодера)
- Меньший LR предотвращает "забывание" предобученных признаков ResNet

**Рекомендации:**
- `1e-5` — стандарт для fine-tuning
- Если encoder переобучается → уменьшите до `5e-6`
- Если encoder не учится → увеличьте до `2e-5` (осторожно!)

**Важно:** Используется только при `lr_mode: "finetune"`.

---

## **grad_clip**

**Тип:** `float`  
**Значение по умолчанию:** `5.0`

Максимальная норма градиента (gradient clipping).

**Влияние:**
- Предотвращает "взрывные градиенты" (exploding gradients)
- Стабилизирует обучение
- `0.0` — отключить clipping (не рекомендуется)

**Рекомендации:**
- `5.0` — стандарт для RNN/LSTM
- Если обучение нестабильно → уменьшите до `1.0-2.0`
- Если градиенты слишком маленькие → увеличьте до `10.0`

**Как работает:**
```python
if grad_norm > grad_clip:
    grad = grad * (grad_clip / grad_norm)
```

---

## **use_amp**

**Тип:** `boolean`  
**Значение по умолчанию:** `true`

Использовать ли автоматическое смешанное обучение (Automatic Mixed Precision, FP16).

**Влияние:**
- `true` — обучение в FP16 (половинная точность)
  - В 1.5-2 раза быстрее на GPU
  - В 2 раза меньше памяти VRAM
  - Минимальная потеря точности
- `false` — обучение в FP32 (полная точность)
  - Медленнее, но максимальная точность

**Рекомендации:**
- Всегда используйте `true` на GPU (почти без потери качества)
- Используйте `false` только для отладки или если возникают проблемы с численной стабильностью

**Важно:** Работает только на GPU (CUDA). На CPU автоматически отключается.

---

## **save_every_epoch**

**Тип:** `boolean`  
**Значение по умолчанию:** `false`

Сохранять ли чекпоинт после каждой эпохи.

**Влияние:**
- `true` — сохраняет `epoch_XXX.pth.tar` после каждой эпохи
  - Больше места на диске
  - Можно вернуться к любой эпохе
- `false` — сохраняет только `best_checkpoint.pth.tar` и `last_checkpoint.pth.tar`
  - Экономия места
  - Достаточно для большинства случаев

**Рекомендации:**
- Используйте `false` для обычного обучения
- Используйте `true` для экспериментов или если нужно сравнить разные эпохи

---

## **checkpoint_dir**

**Тип:** `string`  
**Значение по умолчанию:** `"./checkpoints"`

Папка для сохранения чекпоинтов модели.

**Файлы:**
- `best_checkpoint.pth.tar` — лучшая модель (минимальный val_loss)
- `last_checkpoint.pth.tar` — последняя эпоха
- `epoch_XXX.pth.tar` — конкретная эпоха (если `save_every_epoch: true`)

**Важно:** Папка создается автоматически, если не существует.

---

## **resume_from**

**Тип:** `string` или `null`  
**Значение по умолчанию:** `null`

Путь к чекпоинту для возобновления обучения.

**Использование:**
- `null` — начать обучение с нуля
- `"./checkpoints/last_checkpoint.pth.tar"` — продолжить с последней эпохи
- `"./checkpoints/best_checkpoint.pth.tar"` — продолжить с лучшей модели

**Что восстанавливается:**
- Веса модели
- Состояние оптимизатора
- Состояние scheduler
- Состояние scaler (AMP)
- Номер эпохи
- Лучший val_loss

**Рекомендации:**
- Используйте для продолжения прерванного обучения
- Или для fine-tuning с другими параметрами

---

# 📉 scheduler

## **type**

**Тип:** `string`  
**Значение по умолчанию:** `"ReduceLROnPlateau"`  
**Варианты:** `"ReduceLROnPlateau"` (другие пока не реализованы)

Тип LR scheduler для адаптивного изменения learning rate.

**ReduceLROnPlateau:**
- Уменьшает LR, когда val_loss перестает улучшаться
- Позволяет модели лучше сходиться

**Будущие варианты:**
- `"StepLR"` — уменьшение LR по расписанию
- `"CosineAnnealingLR"` — косинусное затухание

---

## **factor**

**Тип:** `float`  
**Значение по умолчанию:** `0.5`

Во сколько раз уменьшать LR при отсутствии улучшений (для ReduceLROnPlateau).

**Примеры:**
- `0.5` — уменьшить LR в 2 раза (LR → LR/2)
- `0.1` — уменьшить LR в 10 раз (LR → LR/10)
- `0.9` — уменьшить LR на 10% (LR → LR*0.9)

**Рекомендации:**
- `0.5` — стандарт (плавное уменьшение)
- `0.1` — агрессивное уменьшение (быстрее сходимость, но может быть слишком резко)

---

## **patience**

**Тип:** `integer`  
**Значение по умолчанию:** `2`

Количество эпох без улучшения val_loss, прежде чем scheduler уменьшит LR.

**Влияние:**
- Маленький patience → быстрее уменьшение LR (может быть слишком рано)
- Большой patience → медленнее уменьшение LR (может затянуть обучение)

**Рекомендации:**
- `2-3` — стандарт
- `1` — агрессивно (может уменьшить LR слишком рано)
- `5+` — консервативно (может затянуть обучение)

---

## **min_lr**

**Тип:** `float` (научная нотация)  
**Значение по умолчанию:** `1e-7`

Минимально допустимая learning rate (ниже не уменьшается).

**Влияние:**
- Предотвращает слишком маленький LR (когда обучение уже не помогает)
- Обычно очень маленькое значение

**Рекомендации:**
- `1e-7` — стандарт
- `1e-8` — еще меньше (если нужно продолжить обучение очень долго)
- `1e-6` — больше минимум (может остановить уменьшение раньше)

---

# ⏹️ early_stopping

## **enabled**

**Тип:** `boolean`  
**Значение по умолчанию:** `true`

Включить ли раннюю остановку обучения.

**Влияние:**
- `true` — обучение останавливается при отсутствии улучшений или переобучении
- `false` — обучение продолжается до `epochs` эпох

**Рекомендации:**
- Всегда используйте `true` (экономит время и предотвращает переобучение)

---

## **patience**

**Тип:** `integer`  
**Значение по умолчанию:** `5`

Количество эпох без улучшения val_loss, прежде чем остановить обучение.

**Влияние:**
- Маленький patience → быстрая остановка (может быть слишком рано)
- Большой patience → долгая остановка (может переобучить модель)

**Рекомендации:**
- `3-5` — стандарт
- `1-2` — агрессивно (для быстрых экспериментов)
- `7-10` — консервативно (для важных экспериментов)

---

## **min_delta**

**Тип:** `float` (научная нотация)  
**Значение по умолчанию:** `1e-4`

Минимальное изменение val_loss, которое считается улучшением.

**Влияние:**
- Предотвращает остановку при микроскопических улучшениях
- Если улучшение < `min_delta` → не считается улучшением

**Примеры:**
- `val_loss: 2.5000` → `2.4999` (улучшение `0.0001` < `1e-4`) → не считается улучшением
- `val_loss: 2.5000` → `2.4998` (улучшение `0.0002` > `1e-4`) → считается улучшением

**Рекомендации:**
- `1e-4` — стандарт (0.0001)
- `1e-3` — более строгое требование (0.001)
- `1e-5` — более мягкое требование (0.00001)

---

## **track_overfitting**

**Тип:** `boolean`  
**Значение по умолчанию:** `true`

Включить ли отслеживание признаков переобучения.

**Влияние:**
- `true` — мониторит gap между train/val loss и останавливает при переобучении
- `false` — отслеживание отключено

**Рекомендации:**
- Всегда используйте `true` для предотвращения переобучения

---

## **overfitting_gap_threshold**

**Тип:** `float`  
**Значение по умолчанию:** `0.5`

Порог разницы между train и val loss для детекции переобучения.

**Формула:** `gap = train_loss - val_loss`

**Интерпретация:**
- `gap > 0` — норма (train loss выше val loss)
- `gap ≈ 0` — идеально (модель хорошо обобщается)
- `gap < -threshold` — переобучение (val loss значительно выше train loss)

**Примеры:**
- `train_loss: 2.0`, `val_loss: 2.6` → `gap = -0.6` < `-0.5` → переобучение
- `train_loss: 2.0`, `val_loss: 2.3` → `gap = -0.3` > `-0.5` → норма

**Рекомендации:**
- `0.3-0.5` — стандарт
- `0.2` — более чувствительно (раньше детектирует переобучение)
- `0.7-1.0` — менее чувствительно (позволяет больше переобучения)

---

## **overfitting_patience**

**Тип:** `integer`  
**Значение по умолчанию:** `3`

Количество эпох с переобучением (gap < -threshold), прежде чем остановить обучение.

**Влияние:**
- Маленький patience → быстрая остановка при переобучении
- Большой patience → позволяет больше переобучения

**Рекомендации:**
- `2-3` — стандарт
- `1` — агрессивно (остановка сразу при переобучении)
- `5+` — консервативно (позволяет больше переобучения)

---

# 📝 logging

## **tensorboard**

**Тип:** `boolean`  
**Значение по умолчанию:** `false`

Включить ли логирование в TensorBoard.

**Использование:**
```bash
tensorboard --logdir ./runs
```

**Рекомендации:**
- Используйте для визуализации метрик в реальном времени
- Полезно для анализа обучения

---

## **tensorboard_dir**

**Тип:** `string`  
**Значение по умолчанию:** `"./runs"`

Путь к директории для логов TensorBoard.

---

## **use_wandb**

**Тип:** `boolean`  
**Значение по умолчанию:** `false`

Использовать ли Weights & Biases для логирования.

**Требования:**
- Установка: `pip install wandb`
- Авторизация: `wandb login`

**Рекомендации:**
- Используйте для облачного логирования и сравнения экспериментов

---

## **wandb.project**

**Тип:** `string`  
**Значение по умолчанию:** `"image-captioning"`

Название проекта в WandB.

---

## **wandb.entity**

**Тип:** `string` (опционально)  
**Значение по умолчанию:** `"tumambae"`

Опциональное имя workspace/organization в WandB.

---

# 📊 metrics

## **bleu**

**Тип:** `boolean`  
**Значение по умолчанию:** `true`

Включить ли расчет BLEU-score во время валидации.

**Влияние:**
- `true` — рассчитывает BLEU на каждом валидационном шаге
- `false` — только loss (быстрее)

**Рекомендации:**
- Используйте `true` для мониторинга качества генерации
- Отключите только если нужна максимальная скорость

---

## **use_sacrebleu_if_available**

**Тип:** `boolean`  
**Значение по умолчанию:** `true`

Использовать ли библиотеку `sacrebleu` вместо `nltk` (если установлена).

**Преимущества sacrebleu:**
- Более точный расчет
- Стандартизированный алгоритм
- Лучше для сравнения с другими работами

**Fallback:**
- Если `sacrebleu` не установлен → использует `nltk`
- Если `nltk` не установлен → BLEU не рассчитывается

**Установка:**
```bash
pip install sacrebleu
# или
pip install nltk
```

---

## 📚 Дополнительная информация

Для полной документации по использованию проекта см. `README.md` в корне проекта.
